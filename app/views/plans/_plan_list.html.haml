#allplans
  %table.table.table-striped
    %tr.header
      %th= t :id
      %th= t :owner
      %th= t :performer
      %th= t :department
      %th= t :created
      %td
    %tbody
    - @plans.each do |p|
      %tr{id: p.id}
        %td= p.id
        %td= p.id_owner
        %td= p.id_performer
        %td= p.id_department
        %td= p.created_at
        %td
          %button{"onclick"=> "showPlan()"}= t :show
          %button{"onclick"=> "editPlan()"}= t :edit
          %button{"onclick"=> "deletePlan()"}= t :delete
  =link_to t('new plan'), new_plan_path, class: 'btn'
%script
  = render 'plan_list.js'
=will_paginate @plans if @plans

